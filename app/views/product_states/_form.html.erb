<%= form_for(@product_state) do |f| %>
  <% if @product_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_state.errors.count, "error") %> prohibited this product_state from being saved:</h2>

      <ul>
      <% @product_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= t(:name) %>
    <%= f.text_field :name %>  
    <%= f.submit t(:save), class:'btn' %>
  </div>
<% end %>

//$("#cart").html("<%= j render @cart %>");
//$("#current_item").css('background-color', 'red').animate({backgroundColor : "#0CF"}, 1000);
$(".cart_total_quantity").html("<%= @cart.total_quantity %>");
$(".cart_total_price").html("<%= @cart.total_price %>");


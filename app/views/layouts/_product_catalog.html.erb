<h4>Categories</h4>  	
<ul class='nav nav-tabs nav-stacked'>
<% ProductCategory.all.each do |category| %>
    <li><%= link_to "#{category.name}", products_path(category_id: category) %></li>
<% end %>
</ul>
                
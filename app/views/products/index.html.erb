<h1><%= t(".Products") %></h1>
 <%= link_to t(:show_quantity), show_quantity_products_path(products:@products.map(&:id)), class: 'btn btn-primary', remote: true %></br>
<ul class="thumbnails">
    <% @products.each do |product| %>
        <li class='span3'>
            <div class='thumbnail'>
                <%= link_to  product do %>                             
            	   <%= image_tag(product.assets.first.data.url(:medium)) unless product.assets.first.nil? %>
                <% end %>   
                    <h3><%= product.title %></h3>
                    <p>UAN <%= product.price %></p>
                    <p>PLU: <%= product.id %></p>
                    <p id=<%="quantity#{product.id}" %>></p></br>
                    
					<%= link_to 'Edit', edit_product_path(product), class: "btn btn-link" if can? :update, product %>
                    
                    <% if can? :delete, product %>
                    <%= link_to 'Destroy', product, method: :delete, confirm: "Are you sure?", class: "btn btn-link" %>
                    <% end %>                    
                    <%= link_to t('.Add_to_cart'), line_items_path(product_id: product), method: :post, class: 'btn btn-primary', remote: true %>              
            </div>
            
        </li>
    <% end %>
<ul>
<div class='span9 offset3'>
    <%= will_paginate @products %>
    <%= link_to 'New Product', new_product_path, class:'btn btn-success' if can? :create, Product %>
   

</div>

